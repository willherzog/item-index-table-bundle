<?php

namespace WHSymfony\WHItemIndexTableBundle\View;

/**
 * Create a class extending from {@link WHSymfony\WHItemIndexTableBundle\View\ItemTableColumn} that
 * implements this interface to opt into using the sort-by property value of each such column for
 * request queries (instead of the slug value, which is the default behavior).
 *
 * @author Will Herzog <willherzog@gmail.com>
 */
interface UseSortByPropertyForRequests
{
}
